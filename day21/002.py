#JS
#기본타입 : 변수가 값을 담는다
#참조타입(object) : 변수가 값을 가리킨다

#파이썬
#기본타입 : 값이 1개
#집합타입 : list, set(읽기전용 배열)

name = "홍길동"
irum = "전우치"

# print에 파라미터를 여러개주면 공백으로 구분해서 출력
print(name, irum)
print(type(name))

large = 5>3
print(large)
print(type(large))
